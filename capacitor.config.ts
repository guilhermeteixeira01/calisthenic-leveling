import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teixeira.calisthenicleveling',
  appName: 'Calisthenic Leveling',
  webDir: 'build',
};

export default config;
